// const haversine = require('haversine-distance');

// exports.isWithinRadius = (lat1, lon1, lat2, lon2, radius) => {
//   const point1 = { latitude: lat1, longitude: lon1 };
//   const point2 = { latitude: lat2, longitude: lon2 };
//   const distance = haversine(point1, point2); // in meters
//   return distance <= radius;
// };

// exports.isWithinTime = (startTime, currentTime) => {
//   const classStartTime = new Date(startTime).getTime();
//   const difference = (currentTime.getTime() - classStartTime) / (1000 * 60); // in minutes
//   return difference <= 15;
// };
